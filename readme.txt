1)cada programa .go tiene que empezar con una palabra reservada
por ejemplo package main (si el archivo se llama main).

luego hay que poner las librerias con lo que voy a trabajar (usamos el import seguido de los paquetes a usar)
el paquete principal de go es "fmt"

2) go nos borra las cosas que no usamos, o nos las marca

3)la instruccion que se usa en func (para instrucciones que devuelven o no valores)

4) para ejecutar el proyecto: usar la terminal y posicionarnos en la carpeta del mismo
    go run main.go esto compila en memoria y ejecuta el resultado (aca no me compila el programa final)
    go biuld main.go esto compila la aplicacion

5) en GO no existen parentesis, salvo que sea para agrupar datos.